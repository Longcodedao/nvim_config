return {
  'windwp/nvim-autopairs',
  event = "InsertEnter", -- Load only when entering Insert mode
  config = true,         -- Run the default setup() automatically
  -- If you are using cmp, you MUST also add the integration (see step 3)
}
